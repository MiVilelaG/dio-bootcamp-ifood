//faça um programa que fale o nome do restaurante e o tempo estimado da entrega.
const nomeRestaurante = MCDonalds;
const tempoEstimadoEntrega = 10;

let mensagem = `O restaurante ${nomeRestaurante} entrega em ${tempoEstimadoEntrega} minutos.`;

console.log(mensagem);